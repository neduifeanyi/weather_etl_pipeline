# weather_etl_pipeline
An ETL pipeline that extract weather data from a public weather API, transform and load it into a PostgreSQL database. Next, the resulting pipeline is automated to run every one hour. The database can also be queried and the data visualized
